import 'package:flutter/material.dart';

class DashboardSubpage extends StatelessWidget {
  const DashboardSubpage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Dashboard Subpage'),
      ),
    );
  }
}
